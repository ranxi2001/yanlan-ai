# Changelog

本项目遵循语义化版本号。

## 0.4.6 - 2026-07-31

- 录音期间将 MediaRecorder 分片持续提交到 IndexedDB，页面意外关闭后可恢复已落盘音频，并在正常合并后原子清理临时分片
- 修复 IndexedDB 在事务真正提交前误报保存成功的问题；删除当前录音、历史记录截断和本地删除失败均采用更安全的处理
- MiMo 请求增加 120 秒超时、可重试状态识别和指数退避；任一实时片段最终失败时阻止生成不完整纪要，并保留录音供重新转写
- API 响应正文中断会进入重试，携带 Key 的远程模型端点强制使用 HTTPS（本机回环地址除外）
- GPT 校正只接受格式调整和显式术语提示/结构化岗位字段中的完整术语替换，并保留 ASR 说话人归属；会议金句、关键决策和面试能力证据必须匹配对应时间段的原始逐字稿，重复证据全局去重，自动面试结论不再推进或淘汰候选人
- 面试能力项不再保留模型生成的强弱评级或解释，只展示经时间与原文校验的候选证据并要求人工判断；关键决策拒绝在真实引用后拼接的模型结论
- 长逐字稿使用有界批次生成纪要和面试证据，问答检索相关片段；网页上传按区间混音并限制默认 MiMo 解码边界，CLI 在读取前拒绝超过 40 MiB 的 data URL 请求
- CLI 默认拒绝覆盖已有输出，并阻止输出路径通过同路径、符号链接或硬链接覆盖输入音频；显式 `--force` 仅可替换非输入输出文件
- 浏览器端到端测试改为自启动服务和程序化 WAV，不再依赖本机夹具，并在 CI 中覆盖崩溃恢复、重试和完整性阻断

## 0.4.5 - 2026-07-31

- 发布零新增运行时依赖的 `yanlan transcribe` CLI，支持常见音频文件转为纯文本、Markdown 或 JSON
- CLI 默认使用 MiMo-V2.5-ASR，支持语言、Base URL、模型、协议、请求路径和超时配置
- 发布 `yanlan-transcribe` Agent Skill，支持通过标准 Agent Skills 客户端安装和调用
- 增加 CLI 参数、输出、安全失败和 MiMo 请求协议测试

## 0.4.4 - 2026-07-31

- 发布言澜全新青绿色品牌 Logo，融合声纹、字母 Y 与水波涟漪
- 浏览器标签与应用侧栏使用统一品牌图形，README 展示完整英文标志

## 0.4.3 - 2026-07-31

- API Key 改为保存在浏览器 localStorage，刷新或重新打开页面后无需重复填写
- 自动迁移旧版当前标签页中的 sessionStorage Key，并清理旧会话存储
- 设置页增加“清除本机 Key”，并明确 Key 仅发送给用户配置的模型 API
- 无需配置 API Key 即可作为本地录音器使用，支持播放和导出录音文件
- 仅录音记录可在后续配置模型后，从本机原录音继续生成逐字稿
- 设置页增加 GPT 中转站与 MiMo 专属注册链接，并透明展示 MiMo 邀请奖励

## 0.4.1 - 2026-07-31

- 修复首次使用且未填写 API Key 时，设置对话框的关闭和取消按钮被保存校验阻止的问题

## 0.4.0 - 2026-07-31

- 增加概览、会议金句、发言人总结、关键决策和提问五个会议洞察视图
- 为金句和关键决策保留发言人、时间点与逐字稿原话证据
- 扩展 Markdown、JSON、分享链接和离线 HTML 中的结构化会议洞察
- 增加仅监听 127.0.0.1 的同源 API 网关，支持用户自定义的 MiMo 与 GPT Base URL
- 增加网关来源校验、目标协议校验、请求与响应大小限制、超时和集成测试

## 0.3.0 - 2026-07-31

- 文本模型默认切换到 `gpt-5.6-luna` 与 Responses API
- 保留 Chat Completions 和旧版浏览器配置兼容

## 0.2.0 - 2026-07-31

- 增加面试专用模式、岗位能力证据、风险项和下一轮追问
- 增加面试分享稿隐私裁剪与人工复核提示

## 0.1.0 - 2026-07-30

- 发布纯前端 BYOK 会议记录工作台
- 支持 MiMo 实时录音和音频文件分段转写
- 支持 GPT 专有名词校正、摘要、行动项和逐字稿问答
- 支持 IndexedDB 本地录音、时间轴播放、分享和五种导出格式
- 增加桌面与移动端响应式界面、单元测试和浏览器端到端验收
