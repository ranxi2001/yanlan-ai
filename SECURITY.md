# Security Policy

请通过仓库维护者提供的私有渠道报告安全问题，不要公开提交仍可利用的漏洞或任何凭据。

本项目是纯前端 BYOK 应用。运行中的页面能够读取用户在该标签页填写的 API Key，因此应只在可信域名或本地部署中使用。分享链接不包含 Key 和录音，但逐字稿本身可能包含敏感信息；发送链接前请确认接收范围。

携带 API Key 的远程模型端点必须使用 HTTPS。为支持本地开发，仅 `localhost`、`127.0.0.0/8` 和 `[::1]` 回环地址允许使用 HTTP；不要通过明文 HTTP 连接局域网或公网中转站。

CLI 和 `yanlan-transcribe` Skill 从本机读取音频，并将音频直接发送给用户配置的 MiMo 兼容 API。优先使用 `MIMO_API_KEY` 环境变量，不要把 Key 写入命令参数、脚本、日志、Skill 文件或 Git 提交。转写输出默认保留在本机，仍可能包含敏感信息。CLI 默认拒绝覆盖已有输出，并始终拒绝让输出指向输入音频；只有用户明确同意替换非输入文件时才应使用 `--force`。默认 data URL 协议会在读取前拒绝超过 40 MiB 的文件，避免单次内存和请求体失控；大文件应先切分或交给支持标准 multipart 转写的兼容服务。

浏览器录音以约一秒为单位增量写入 IndexedDB。意外刷新或崩溃后，言澜会恢复已经提交的连续分片，但尚未触发 `dataavailable` 的最后一个时间片仍可能丢失。重要会议应确认页面显示“录音已保存在本机”并及时导出备份。

GPT 校正和结构化纪要属于不可信模型输出。应用只接受标点/大小写调整和用户通过标签明确给出的完整术语替换，保留 ASR 说话人归属，并验证带时间证据的引用是否存在于对应原始逐字稿片段。关键决策只保留决策型原话中的精确表述；面试能力项只展示候选证据，并固定为“有待确认”或“证据不足”。程序无法证明原话足以支持能力判断，绝不自动推进/淘汰候选人。
