# Security Policy

请通过仓库维护者提供的私有渠道报告安全问题，不要公开提交仍可利用的漏洞或任何凭据。

本项目是纯前端 BYOK 应用。运行中的页面能够读取用户在该标签页填写的 API Key，因此应只在可信域名或本地部署中使用。分享链接不包含 Key 和录音，但逐字稿本身可能包含敏感信息；发送链接前请确认接收范围。

CLI 和 `yanlan-transcribe` Skill 从本机读取音频，并将音频直接发送给用户配置的 MiMo 兼容 API。优先使用 `MIMO_API_KEY` 环境变量，不要把 Key 写入命令参数、脚本、日志、Skill 文件或 Git 提交。转写输出默认保留在本机，仍可能包含敏感信息。
